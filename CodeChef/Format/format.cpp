#include <iostream>
#include <string.h>

using namespace std;

int main()
{
    int t;
    cin >> t;
    while (t > 0)
    {
        int n, k;
        cin >> n >> k;

        for (int i = 0; i < n; i++)
        {
        }

        t--;
    }
    return 0;
}
