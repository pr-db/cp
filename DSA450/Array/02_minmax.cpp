#include <iostream>
#include <cstdlib>
#include <string>
#include <set>

using namespace std;

struct minmax
{
	int max;
	int min;
};

int main()
{
	// ios_base::sync_with_stdio(false);
	// cin.tie(NULL);
	int t;
	cin >> t;
	while (t > 0)
	{
		int n, k;
		cin >> n >> k;

		for (int i = 0; i < n; i++)
		{
		}

		t--;
	}
	return 0;
}
